{
  "name": "stend-history",
  "version": "0.1.0",
  "description": "Undo/redo history helper for stores with get/set/subscribe (e.g., Stend).",
  "license": "GPL-3.0-only",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --pretty",
    "clean": "rm -rf dist",
    "prepare": "npm run build",
    "test": "vitest"
  },
  "keywords": ["stend", "state", "history", "undo", "redo"],
  "devDependencies": {
    "@types/node": "^20.12.11",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
